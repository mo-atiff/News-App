//write a c program , to find type of a triangle by reading three sides using only if
//wcp to find vol. of cuboid if the height is less than or equal to 40 else print an error using if-else
//wcp to find grade by reading 5-subs marks fron user and print full student info [name, srn, avg-marks, grade] in tabular form using if-else ladder
//wcp to compare 2 ints and print result read_values of two vars from input [nested - if]

// wcp to count no of [lines,chars,words] in a file
// wcp to prod all digits in a num
// all algebra using switch case
// perform complex nums arithematic operations using switch case

// #include <stdio.h>
// int main()
// {
//     int a, n, prod=1,sum;
//     printf("Enter Numbers : ");
//     scanf("%d", &a);

//     while (a!=0)
//     {
//         sum = (a % 10);
//         prod*=sum;
//         a = a/10;


//     }
//     printf("%d", prod);

//     return 0;
    
// }
#include <stdio.h>
int main()
{
    // int a, b;
    // char c;
    // printf("Enter Operation (+, -, *, /) : ");
    // scanf("%c", &c);
    // printf("Enter Two numbers : ");
    // scanf("%d %d", &a, &b);

    // switch (c)
    // {
    // case '+':
    // printf("%d", a+b);
    // break;

    // case '-':
    // printf("%d", a-b);
    // break;

    // case '*':
    // printf("%d", a*b);
    // break;

    // case '/':
    // printf("%d", a/b);
    // break;

    // case '%':
    // printf("%d", a%b);
    // break;
    // }


    return 0;
}
